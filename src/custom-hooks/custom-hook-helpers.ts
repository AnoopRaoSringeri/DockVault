import { Folder } from "../models/models";
import { PersistValue } from "./use-folder-params";

export const GetPathForFolder = (
  folders: Folder[],
  folderId: number
): PersistValue | null => {
  const path: PersistValue | null = null;

  return path;
};
